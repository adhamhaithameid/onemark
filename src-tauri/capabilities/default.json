{
  "$schema": "https://schema.tauri.app/capability/2",
  "identifier": "default",
  "description": "Spike capability: core window/IPC only. Real M2 adds dialog and fs plugin permissions here.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
