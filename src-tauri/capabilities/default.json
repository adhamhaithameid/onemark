{
  "$schema": "https://schema.tauri.app/capability/2",
  "identifier": "default",
  "description": "M2 capability set: core window/IPC, folder picking (dialog) and folder read/write (fs). Files are scoped at runtime to what the user picks through the dialog — no broad filesystem access.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "fs:allow-read-dir",
    "fs:allow-read-text-file",
    "fs:allow-write-text-file",
    "fs:allow-remove",
    "fs:allow-stat"
  ]
}
