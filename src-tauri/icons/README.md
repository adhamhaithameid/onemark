Icons: run `cargo tauri icon` (tauri-cli, M2 setup) to generate the full
platform set (icns/ico). The spike ships a placeholder 32x32 PNG so the
config compiles; bundling is disabled in tauri.conf.json.
