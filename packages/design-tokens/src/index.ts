/**
 * @onemark/design-tokens — typed access to the token data (ADR-0017).
 * The CSS side lives in css/tokens.css (generated); this re-exports the
 * generated TS constants.
 */
export * from './tokens';
